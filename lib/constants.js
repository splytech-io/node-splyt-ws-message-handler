'use strict';

const TYPE_REQUEST = 'request';
const TYPE_PUSH = 'push';
const TYPE_RESPONSE = 'response';
const TYPES = [
  TYPE_REQUEST,
  TYPE_PUSH,
  TYPE_RESPONSE,
];

module.exports = {
  TYPES,
  TYPE_REQUEST,
  TYPE_PUSH,
  TYPE_RESPONSE,
};
